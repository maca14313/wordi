import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wordi.app',
  appName: 'Wordi',
  webDir: 'dist/project-test/browser'
};

export default config;
